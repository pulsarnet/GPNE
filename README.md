# GPNE - Graphical Petri Net Editor

# Prepare

After clone, you must init submodules
```
git submodule update --init --recursive
```
# Requirements
- QT (>=6.8)
- Rust language (>=1.83)

# Build

```shell
cmake --build --preset release 
```